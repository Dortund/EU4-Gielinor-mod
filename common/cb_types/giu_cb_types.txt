# INSTRUCTIONS:
# -------------
# is_triggered_only - Triggered from within the code or by event effects. DO NOT REMOVE THE STOCK ONES.
# months - The number of months this CB will be valid. Only works for triggered CBs.
# prerequisites - A condition that automatically activates a CB. Does not work for triggered CBs.
# no_opinion_hit - If yes you get no negative opinion from this
#
# ROOT = attacker
# FROM = target
#
# NOTE: The order in which the peace options are listed is the order in which the AI will normally prioritize them in peace treaties
#
# TRIGGERED - Triggered from within the code or by event effects
# --------------------------------------------------------------

# Amascut Revenge
cb_cleanse_corruption = {
	valid_for_subject = no
	holy_war = yes
	is_triggered_only = yes
	months = 60

	prerequisites = {
		religion = menaphite
		FROM = { religion = amascut_religion }
	}
	
	attacker_disabled_po = {
		po_change_religion
		po_demand_provinces
		po_revoke_cores
		po_release_vassals
		po_release_annexed
		po_return_cores
		po_become_vassal
		po_subjugate_vassal
		po_form_personal_union
		po_transfer_vassals
	}

	war_goal = goal_cleanse_amascut_corruption
}


# Kourend House broke free
cb_restore_kourend_house = {
	valid_for_subject = no

	prerequisites = {
		OR = {
			has_reform = kourend_monarchy
			has_reform = kourend_reformed_monarchy
			has_reform = kourend_council
		}
		is_revolutionary = no
		FROM = {
			has_reform = kourend_independent_house
			is_subject = no
		}
	}

	war_goal = goal_take_capital_disloyal_house
}

#Kourend - War of Five Houses
cb_reunite_kourend = {
	valid_for_subject = no

	prerequisites = {
		NOT = { exists = KOU }
		OR = {
			tag = AUU
			tag = HOS
			tag = KTW
			tag = LOV
			tag = PCL
			tag = SYZ
		}
		is_revolutionary = no
		FROM = {
			any_owned_province = {
				region = kourend_region
			}
			is_subject = no
			or = {
				tag = AUU
				tag = HOS
				tag = KTW
				tag = LOV
				tag = PCL
				tag = SYZ
			}
		}
	}
	war_goal = goal_reunite_kourend
}

cb_runecrafting_crusade = {
	valid_for_subject = no

	prerequisites = {
		has_reform = runecrafting_crusader
		current_age = age_of_humans
		FROM = {
			any_owned_province = {
				trade_goods = runes
				NOT = { has_province_modifier = GUN_ruined_runecrafting_altar }
			}
		}
	}
	war_goal = goal_take_runecrafting_altars
}

cb_unify_asgarnia = {
	valid_for_subject = no

	prerequisites = {
		NOT = { exists = ASG }
		NOT = { tag = ENT }
		NOT = { tag = CDR }
		mission_completed = genasg_decent_army
		culture_group = asgarnian_group
		capital_scope = { region = asgarnia_region }
		is_revolutionary = no
		FROM = {
			any_owned_province = {
				AND = {
					region = asgarnia_region
					NOT = {
						area = entrana_area
						area = crandor_area
						province_id = 52
					}
				}
			}
		}
	}
	war_goal = goal_unify_asgarnia
}

